# /safe-refactor

Use the `safe-refactor` skill.

Rules:

- Keep behavior unchanged.
- Prefer tests before refactor.
- Split refactor from feature work.
- For broad refactors, ask `refactor-planner` for a staged plan first.

