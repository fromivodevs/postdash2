# Preset: anthropic-sdk-app

Type: overlay preset.

Detect when imports or dependencies mention:

- `anthropic`
- `@anthropic-ai/sdk`

Add skills:

- `api-cache-tune`

Behavior:

- Apply this on top of a base preset such as `python-backend`, `nextjs-app`, or `nodejs-cli`.

