/**
 * Command handlers.
 *
 * Phase 0: placeholder.
 * Phase 1+: CreateWorkspace, LinkTelegramIdentity, ConnectContentChannel,
 *           AddSourceSubscription, GenerateDraft, RewriteDraft, PublishPost.
 *
 * Критичные команды идемпотентны через `command_idempotency` таблицу
 * (см. tg_mvp_plan/02-ARCHITECTURE.md Rule 10).
 */

export {};
