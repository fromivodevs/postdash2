/**
 * Policy / authorization checks (central, не размазаны по handler'ам).
 *
 * Phase 0: placeholder.
 * Phase 1+: identity, workspace membership, role, ownership, rate-limit,
 *           cross-workspace integrity invariant (draft.workspace_id == channel.workspace_id).
 *
 * См. tg_mvp_plan/05-SECURITY-AND-ACCOUNTS.md.
 */

export {};
