# /roadmap

Use the `roadmap-keeper` skill.

Update:

- `PROJECT_MAP.md`
- `ARCHITECTURE.md`
- `architecture/<system>.md` when a new system appears

If context is tight, delegate to `roadmap-keeper-agent`.

