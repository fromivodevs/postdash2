# /preflight

Use the `pre-flight-check` skill before non-trivial code changes.

Check:

- git status
- current branch
- project instructions in `CLAUDE.md`
- navigation files: `PROJECT_MAP.md`, `ARCHITECTURE.md`
- relevant system docs under `architecture/`

