# /dep-audit

Use the `dep-audit` skill before adding dependencies.

Check:

- package size and transitive dependencies
- maintenance/release freshness
- license
- security advisories
- native build requirements
- simpler standard-library or existing-project alternatives

