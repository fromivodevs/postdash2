---
name: "api-contract"
description: "Reviews API contract changes: DTOs, validation, status codes, errors, versioning, compatibility. Trigger: `/api-contract`, \"проверь API контракт\"."
trigger_patterns:
  - "/api-contract"
  - "api contract"
  - "проверь API контракт"
---

# API Contract

Use `api-contract-reviewer`.

Report contract changes, breaking risks, missing validation, tests, and docs.

