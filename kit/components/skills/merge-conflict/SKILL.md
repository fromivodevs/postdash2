---
name: "merge-conflict"
description: "Resolves merge conflicts conservatively while preserving both sides' intent. Trigger: `/merge-conflict`, \"разрули конфликт\"."
trigger_patterns:
  - "/merge-conflict"
  - "разрули конфликт"
  - "merge conflict"
---

# Merge Conflict

Use `merge-conflict-resolver`.

Understand both sides, preserve user changes, edit only conflict areas, and run focused checks.

